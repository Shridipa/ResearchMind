# Models

Store model cards, local adapters, and embedding comparison notes here.

Recommended baseline:

- `sentence-transformers/all-MiniLM-L6-v2` for fast local retrieval.
- `BAAI/bge-base-en-v1.5` for stronger retrieval experiments.
- Cross-encoder reranker for top-k reranking.
